# Ignore the folders that are generated by hardhat / truffle (if we choose to use)
/build
/migrate
/scripts
/test

# Ignore the outputs of our python test program that prints sample data
/TestingCommon/*csv*